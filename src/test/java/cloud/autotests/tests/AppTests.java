package cloud.autotests.tests;

import io.qameta.allure.Description;
import org.junit.jupiter.api.*;

import static cloud.autotests.helpers.DriverHelper.getConsoleLogs;
import static com.codeborne.selenide.Selenide.*;
import static io.qameta.allure.Allure.step;
import static org.assertj.core.api.Assertions.assertThat;


public class AppTests extends TestBase {
    @Test
    @Description("Soon to be implemented by QA.GURU engineers")
    @DisplayName("Create play list")
    void generatedTest() {
        step("1 Open spotify.com", () -> {
            // todo just add selenium action
        });

        step("2 Push 'Log in' button", () -> {
            // todo just add selenium action
        });

        step("3 Fill 'email or username' and 'password' fields", () -> {
            // todo just add selenium action
        });

        step("4 push 'Log In'", () -> {
            // todo just add selenium action
        });

        step("5 close premium popup", () -> {
            // todo just add selenium action
        });

        step("6 Select Create playlist", () -> {
            // todo just add selenium action
        });

        step("7 Rename", () -> {
            // todo just add selenium action
        });

        step("8 Add descrpition", () -> {
            // todo just add selenium action
        });

        step("9 save", () -> {
            // todo just add selenium action
        });

        step("10 assure that playlist created", () -> {
            // todo just add selenium action
        });
    }
}